eureka:
  client:
    service-url:
      defaultZone: http://jason-dell:10010/eureka/,http://jason-dell:10011/eureka/

spring:
  cloud:
    config:
      label: master
      profile: matrix-web-front-page-env
      discovery:
        serviceId: matrix-config-server
        enabled: true